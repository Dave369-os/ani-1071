## Rapport:
```
Bien que nous ayons un ordinateur portable fonctionnant sous windows, nous avons utilise la commande s'apparentant a Unix car nous utilisons pour les exercices de monsieur le terminal Gibash(qui selon des recherches approfondies ne fonctionnerait qu'avec des commandes valables sur Unix). Ainsi,avec '&&', nous avec teste 4 nombres dont les resultats recues sont les suivants:
-
$ ./test.exe && echo pair
4
pair

-
$ ./test.exe && echo pair
3
(Dans ce cas-ci, aucune sortie n'a ete enregistree. Car la sortie ne verifie pas la condition)

-
$ ./test.exe && echo pair
10210
pair

Cette ligne ressemble a un systeme de construction car elle enchaine compilation, test, action et s'arrete des qu'une etape echoue.
```